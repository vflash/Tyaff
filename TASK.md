# TASK.md — План работ AIDEV

> План багфиксов по результатам ревью `src/core.js`
> Каждый баг подтверждён воспроизводящим тестом на happy-dom.
>
> **Правило (TDD):** сначала падающий тест → потом фикс → потом `npm test` зелёный.

## Легенда

- 🔴 **критический** — крэш / потеря контента / вечный hang
- 🟠 **средний** — нарушение lifecycle / молчаливое неверное поведение
- 🟡 **расхождение со спекой** — нужно решение: чинить код или спеку
- 🔵 **техдолг** — perf / утечки / чистота

---

## ✅ Этап 1 — Порталы: крэш + lifecycle (баги 1 и 4 вместе, один участок кода) — ИСПРАВЛЕНО

### Баг 1 🔴 — memo-skip + Portal → ReferenceError
- **Статус:** Крэш `ReferenceError: version is not defined` **ИСПРАВЛЕН** (переменная `version` теперь корректно передается)
- **Где:** `refreshMemoSubtree`, строка ~1043 (бывш. 1098)
- **Суть:** ветка Portal вызывает `reconcilePortalChildren(..., version, ...)` — переменная `version` не существует в этом скоупе
- **Воспроизведение:** компонент с `memo()` + портал в поддереве → `update()` без смены зависимостей → `ReferenceError: version is not defined`
- **Сателлиты (ИСПРАВЛЕНО):**
  - Туда передаётся свежий `new Map()` вместо родительского keyMap → дети портала пересоздавались бы (нарушение «Portal использует родительский keyMap» из DEVNOTES)
  - При ошибке в `doRerender` промис `update()` не резолвится никогда (resolvers не флашатся в catch-ветке `_rerender`) → вечный `await`

### Баг 4 🔴 — двойной onUnmounted при исчезновении контейнера портала — ИСПРАВЛЕНО
- **Где:** `reconcilePortalChildren` case 2 (~строка 957)
- **Суть:** `unmountVdom(inst[_RENDERED])` + родительский cleanup по version вызывает `unmountVdom` повторно → `onUnmounted` ×2, `ref(null)` ×2
- **Фикс:** пометить детей `_v = version` и отдать unmount cleanup'у (или удалять из keyMap на месте)

## ✅ Этап 2 — refresh() и Fragment (баг 2) — ИСПРАВЛЕНО

### Баг 2 🔴 — refresh() не находит компоненты внутри Fragment
- **Статус:** ✅ **ИСПРАВЛЕНО** (2026-07-19)
- **Где:** `collectAllInstances`, строка ~1118
- **Суть:** для Fragment обходил `node._nodes` (DOM-узлы) вместо `node.childs` (vnode)
- **Фикс:** заменён обход на `node.childs` в блоке `if (node.tag === Fragment)`
- **Тест:** `tests/test-dev-03.js` ✅
- **Нарушает:** SPEC §13

## ✅ Этап 3 — Вложенные массивы (баг 3) — ИСПРАВЛЕНО

### Баг 3 🔴 — сырые строки/числа во вложенных массивах теряются молча
- **Статус:** ✅ **ИСПРАВЛЕНО** (2026-07-20)
- **Где:** `h()` нормализует только верхний уровень; `reconcile2` дропает примитивы
- **Воспроизведение:** `h('div', null, ['a', 'b', 42])` → пустой textContent
- **Варианты фикса:**
  - A: рекурсивная нормализация в `h()` — лишний проход + аллокации (DEVNOTES: h() in-place ради perf)
  - B: `reconcile2` принимает примитивы как текст на лету — дешевле, **предпочтительно**
- **Нарушает:** SPEC §1 (текстовые узлы обязаны оборачиваться)
- **Фикс:** вариант B — `reconcile2` принимает примитивы как текст на лету
- **Тест:** `tests/test-spec-08.js` ✅

## ✅ Этап 4 — Пересоздание по критическим атрибутам (баг 5) — ИСПРАВЛЕНО

### Баг 5 🟠 — recreate ломает lifecycle детей

**Статус:** ✅ **ИСПРАВЛЕНО** — удалена логика shouldRecreate, теперь React-like поведение (просто обновляем атрибуты без пересоздания DOM)
- **Где:** `reconcile2HTML`, строки ~793-797 (`type` у input/button, `multiple` у select, `is`)
- **Суть:** `unmountVdom(oldElement)` вызывает `onUnmounted`/`ref(null)` у детей, но дети остаются в keyMap и переиспользуются → `onUnmounted` без `onMounted`, instance в полуразмонтированном состоянии
- **Воспроизведение:** `<input type={t}>{h(Child)}</input>`, смена `text`→`password` → события Child: `["unmounted"]`, точка
- **Идея фикса:** при recreate удалять поддерево старого элемента из keyMap до reconcile детей (или unmount после reconcile — обсудить)

## ⚠️ Этап 5 — Расхождения со спекой (нужно решение)

| # | Код | SPEC | Статус |
|---|-----|------|--------|
| E 🟡 | `ref` портала вызывается на каждом render (~стр. 1015) | §6: только mount/unmount | **ИСПРАВЛЕНО** |
| F 🟡 | `update({})` → `true` | §5: `update({})` → `false` | **ИСПРАВЛЕНО** (оптимизация: один цикл, проверка changed) |
| G 🟡 | Без контейнера дети портала не строятся, `init()` отложен | §7: eager init в памяти | **ОСТАВЛЕНО КАК ЕСТЬ** (поведение кода разумнее, закреплено тестом) |

## ⚠️ Этап 6 — Perf и техдолг — НЕ ИСПРАВЛЕНО

- 🔵 text reuse не инкрементирует `keyMap._count` (~стр. 690-705, 833-850) → лишний cleanup-scan O(keyMap) на каждый render списков (возможно, часть отставания в «Update all 5000»)
- 🔵 `mountedContainers` — strong Set → утечка контейнеров без `mount(null)` (SPEC §12 обещает GC)
- 🔵 Висящие updateResolvers при ошибке (общий фикс с багом 1) — **ИСПРАВЛЕНО**
